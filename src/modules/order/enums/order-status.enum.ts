export enum OrderStatus {
  Pending = "pending",
  Canceled = "canceled",
  Paid = "paid",
  Done = "done",
}

export enum OrderItemStatus {
  Pending = "pending",
  Canceled = "canceled",
  Sent = "sent",
}